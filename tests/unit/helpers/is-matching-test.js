import { isMatching } from 'american-adventurers-guild/helpers/is-matching';
import { module, test } from 'qunit';

module('Unit | Helper | is matching');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isMatching([42]);
  assert.ok(result);
});
